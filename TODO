src/channel.c:221:      /* FIXME */
src/channel.c-222-      /* g_cfg->tcfg = file_to_tconfig(net->chanfile); */
--
src/channel.c:287:					/* FIXME: Just update modes if needed and continue */
src/channel.c-288-					break;
--
src/default_triggers.c:47:		/* FIXME: Shouldn't be here*/
src/default_triggers.c-48-		trigger_list_add(&net->trigs->msg,new_trigger(NULL,TRIG_MSG,"rehash",NULL,&rehash_bot));
--
src/default_triggers.c:147:			/* FIXME: Need check for whether they're a recognized user in the bot */
src/default_triggers.c:148:			/* FIXME: Check if nick is bot, if so do WHO, and MODE */
src/default_triggers.c-149-				
--
src/default_triggers.c:159:	/* FIXME: If chan is NULL, might be a freenode-like redirection. Handle it. */
src/default_triggers.c-160-	if (chan == NULL)
--
src/default_triggers.c:193:  /* FIXME: If chan is NULL, might be a freenode-like redirection. Handle it. */
src/default_triggers.c-194-  if (chan == NULL)
--
src/egg_lib.c:538:   * FIXME
src/egg_lib.c-539-   */
--
src/egg_lib.c:542:		troll_debug(LOG_ERROR, "FIXME: egg_matchattr() only works for global flags");
src/egg_lib.c-543-  } 
--
src/egg_lib.c:942:    /* Needs to check stackable, and whether dupes exist FIXME */
src/egg_lib.c-943-    if (!strcmp("pub",type))
--
src/egg_lib.c:1101:				/* Bug, cannot NULL out trigs here, they aren't being freed either FIXME */	
src/egg_lib.c-1102-#ifdef HAVE_TCL
--
src/egg_lib.h:19:/* This is a band-aid function to make an arg to match against FIXME */
src/egg_lib.h-20-char *egg_makearg(char *rest, char *mask);
--
src/network.c:57:		/* FIXME: free fucking JS shit, PHP shit, etc */
src/network.c-58-
--
src/tcl_embed.c:108:/* needs fixed FIXME */
src/tcl_embed.c-109-void tcl_handler(struct network *net, struct trigger *trig, struct irc_data *data, struct dcc_session *dcc, const char *dccbuf)
--
src/tcl_lib.c:23:	/* FIXME: Memory Leak TCL_DYNAMIC for the free func causes crash */
src/tcl_lib.c-24-	Tcl_SetResult(interp, ret, NULL);
--
src/tcl_lib.c:175:  /* No channel flag support FIXME */
src/tcl_lib.c-176-  ret = Tcl_NewIntObj(egg_matchattr(net,Tcl_GetString(objv[1]),Tcl_GetString(objv[2]),NULL));
--
src/user.c:150:					user = new_user(tmp->value,NULL,NULL,NULL,NULL,NULL,NULL); /* FIXME: That's retarded */
--
src/util.c:28:      /* Need better return values FIXME */
src/util.c-29-      return 1;
--
src/util.c:52:      /* Need better return values FIXME */
src/util.c-53-      return 1;
