#ifdef HAVE_CONFIG_H
#include "config.h"
#endif /* HAVE_CONFIG_H */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include <libpq-fe.h>

#include "util.h"
#include "tconfig.h"

#include "pgsql_log_filter.h"

void pgsql_log_filter_handler(struct network *net, struct log_filter *filter, struct log_entry *entry)
{
}

int pgsql_log_filter_start(struct tconfig_block *tcfg)
{
	printf("Started\n");
}

int pgsql_log_filter_start(struct tconfig_block *tcfg)
{
}

int pgsql_log_filter_start(struct tconfig_block *tcfg)
{
}

int pgsql_log_filter_start(struct tconfig_block *tcfg)
{
}
